name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # CI temporarily disabled
  # # lint:
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       python-version: ['3.9', '3.10', '3.11', '3.12']

  #   steps:
  #   - uses: actions/checkout@v4

  #   - name: Set up Python ${{ matrix.python-version }}
  #     uses: actions/setup-python@v4
  #     with:
  #       python-version: ${{ matrix.python-version }}

  #   - name: Cache pip dependencies
  #     uses: actions/cache@v3
  #     with:
  #       path: ~/.cache/pip
  #       key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}
  #       restore-keys: |
  #         ${{ runner.os }}-pip-

  #   - name: Install dependencies
  #     run: |
  #       python -m pip install --upgrade pip
  #       pip install -e .
  #       pip install flake8 mypy black isort

  #   - name: Lint with flake8
  #     run: |
  #       flake8 src/facet/ --count --select=E9,F63,F7,F82 --show-source --statistics
  #       flake8 src/facet/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

  #   - name: Type check with mypy
  #     run: |
  #       mypy src/facet/ --ignore-missing-imports --no-strict-optional --disable-error-code=no-untyped-def --disable-error-code=var-annotated --disable-error-code=assignment --disable-error-code=unreachable --disable-error-code=operator

  #   - name: Format check with black
  #     run: |
  #       black --check src/facet/

  #   - name: Import sort check with isort
  #     run: |
  #       isort --check-only --diff src/facet/

  # build:
  #   runs-on: ubuntu-latest
  #   needs: lint

  #   steps:
  #   - uses: actions/checkout@v4

  #   - name: Set up Python
  #     uses: actions/setup-python@v4
  #     with:
  #       python-version: '3.11'

  #   - name: Install build dependencies
  #     run: |
  #       python -m pip install --upgrade pip
  #       pip install build twine setuptools wheel

  #   - name: Build package
  #     run: python -m build

  #   - name: Check package
  #     run: python -m twine check dist/*
